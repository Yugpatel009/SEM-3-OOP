/**
 * The fixed set of actions the MiniBank menu offers.
 * An enum makes the choices type-safe and easy to switch over.
 */
public enum MenuOption {
    OPEN_ACCOUNT, DEPOSIT, WITHDRAW, TRANSFER, EXIT
}
