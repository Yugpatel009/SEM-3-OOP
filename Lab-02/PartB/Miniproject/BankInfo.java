/**
 * A record holding read-only information about the bank.
 * This single line auto-generates the constructor, the accessors name() and
 * branch(), and equals()/hashCode()/toString().
 */
public record BankInfo(String name, String branch) { }
